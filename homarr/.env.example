# =============================================================================
# Homarr - Deployment Configuration Template
# =============================================================================

# Encryption key used to protect credentials within Homarr.
# Must be a secure string (e.g., generated with 'openssl rand -hex 32').
# Replace this with your actual key in the .env file.
SECRET_ENCRYPTION_KEY=your_secure_encryption_key_here
